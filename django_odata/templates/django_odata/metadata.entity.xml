<EntityType Name="{{entity.name}}">
<Key>
	<PropertyRef Name="{{entity.key_name}}"/>
</Key>
{% for prop in entity.fields %}
<Property Name="{{prop.name}}" Type="{{prop.edm_type}}" Nullable="{{prop.nullable}}"/> 
{% endfor %}
{% for nav in entity.relationships %}
{% if nav.many %}
<NavigationProperty Name="{{nav.name}}" Type="Collection({{nav.related_model_name}})"/>
{% else %}
<NavigationProperty Name="{{nav.name}}" Type="{{nav.related_model_name}}"/>
{% endif %}
{% endfor %}
</EntityType>